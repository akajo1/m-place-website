import React from 'react'
import { colors } from '../styles/colors'

type Props = {}

export default function Inscription({}: Props) {
  return (
    <div style={{color:colors.white}}>inscription</div>
  )
}